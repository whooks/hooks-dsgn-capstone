## What & why

<!-- A short description of the change and the problem it solves. -->

## How it was tested

<!-- Tests written first (TDD)? What did you run? -->

## Checklist

- [ ] Tests were written first and pass (`npm test`)
- [ ] Coverage stays ≥ 80% (`npm run test:coverage`)
- [ ] `npm run validate` is clean (type-check + lint)
- [ ] No file exceeds 300 lines; no secrets committed
- [ ] Docs/rules updated if the architecture, env vars, or API contracts changed
