/**
 * Starter queries for the graph search explorer. They're phrased the way a
 * student might actually ask about themselves, so the auto search has something
 * concrete to compose a context block from. Deliberately avoid the words
 * "summary"/"memory"/"search" so the explorer's buttons stay distinct from the
 * page's other controls.
 */
export const SAMPLE_QUERIES: string[] = [
  'What are my interests and goals?',
  'What projects am I working on?',
  'What decisions have I made recently?',
  'Who and what have I talked about?',
  'What problems am I trying to solve?',
];
